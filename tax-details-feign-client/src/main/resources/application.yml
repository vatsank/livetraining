server:
  port: 9090

spring:
  application:
    name: feign-client  
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka  

